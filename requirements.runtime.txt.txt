# For the VM / Docker mimic

# Core
numpy==1.26.4
pandas==2.2.2
pytz==2024.1

# Dashboard
streamlit==1.37.1
altair==5.3.0
psutil==5.9.8

# Inference (choose ONE path; both pinned for portability)
# 1) Light & fast on manylinux wheels (preferred if wheel resolves):
tflite-runtime==2.13.0

# 2) Fallback to full TensorFlow (comment the line above and uncomment below)
# tensorflow==2.13.1
